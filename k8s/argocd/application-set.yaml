apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: argodemo-appset
  namespace: argocd
spec:
  generators:
    - git:
        repoURL: "https://github.com/moredrowsy/argodemo.git"
        revision: feature/ubuntu-k3s
        files:
          - path: "k8s/helm/chart/envs/**/config.json"
  template:
    metadata:
      name: "argodemo-{{ path[3] }}-{{ path[4] }}-{{ server }}"
    spec:
      project: default
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=false
      source:
        repoURL: "https://github.com/moredrowsy/argodemo.git"
        targetRevision: feature/ubuntu-k3s
        path: k8s/helm/chart
        helm:
          valueFiles:
            - "envs/{{ path[3] }}/{{ path[4] }}/values.yaml"
      destination:
        server: "https://kubernetes.default.svc"
        namespace: default
